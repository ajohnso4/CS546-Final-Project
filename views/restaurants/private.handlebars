<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" />
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/public/css/site.css" />
</head>

<body>
<section>
    <nav class="topnav">
        {{!-- <a  href="/">Home</a> --}}
        <a  class="active" href="/restaurants/private">Profile</a>
        <a href="/restaurants/reservation">Reservations Made</a>
        <a href="/reviews/restaurant/{{restaurant._id}}">Reviews</a>
    </nav>
</section>

<main>
        <div class="jumbotron">
            <div class="container">
                <h1>Welcome, {{restaurant.name}}</h1>
            </div>
        </div>

<section>
    <div class="container">
            <fieldset>
                <legend>Your Restaurant Profile</legend>
    <div class="form-group">
        <label for="name">Restaurant Name</label>
        <input type="text" class="form-control" id="name" value={{restaurant.name}} readonly/>
    </div>

    <div class="form-group">
        <label for="name">Restaurant Website Link</label>
        <input type="url" name="website" class="form-control" value={{restaurant.website}} readonly/>
    </div>
    <div class="form-group">
        <label for="address">Address</label>
        <input type="text" class="form-control"  id="address" value={{restaurant.address}} readonly/>
    </div>

    <div class="form-group">
        <label for="email">Email ID</label>
        <input type="email" class="form-control"  id="email" value={{restaurant.email}} readonly />
    </div>

    <div class="form-group">
        <label for="phone">Contact Number</label>
        <input type="number" class="form-control"  id="phone" value={{restaurant.phone}} readonly/>
    </div>

    <div class="form-group">
        <label for="description">About</label>
        <textarea rows="4" class="form-control" readonly> {{restaurant.description}} </textarea>
    </div>

    <div>
        <p align="center"><a href="/restaurants/logout" class="fcc-btn">Logout</a></p>
    </div>
    </fieldset>
    </div>
</section>
</main>
</body>
</html>


